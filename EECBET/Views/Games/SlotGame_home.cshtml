<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
<link rel="stylesheet" href="~/css/slotgameBase.css" asp-append-version="true" />
<link rel="stylesheet" href="~/css/slotgameSwiper.css" asp-append-version="true" />
@{
    Layout = "~/Views/Shared/_SlotLayout.cshtml";
    ViewBag.Title = "特色遊戲 SlotGame_home";
}
@model IEnumerable<EECBET.Models.GameListViewModel>

<div class="wrap">
    <div class="content">
        <!-- 最新 -->
        <section>
            <div class="sectionTitle">
                <div>
                    <h3>最新</h3>
                </div>
            </div>
            <div class="swiper mySwiper">
                <div class="swiper-wrapper">
                    @foreach (GameListViewModel g in Model)
                    {
                        <div class="swiper-slide" data-release-date="@g.ReleaseDate?.ToString("yyyy-MM-dd")">
                            <a href="#"><img src="@g.GameImageUrl" alt="@g.GameNameEN" /></a>
                        </div>
                    }
                </div>
                <!-- 左右箭頭 -->
                <div class="swiper-button-next"></div>
                <div class="swiper-button-prev"></div>
            </div>
        </section>
        <!-- 獨創 -->
        <section>
            <div class="sectionTitle">
                <div>
                    <h3>獨創</h3>
                </div>
                <div>
                    <a class="more" asp-controller="Games" asp-action="SlotGame_Exclusive">更多</a>
                </div>
            </div>

            <!-- Swiper -->
            <div class="swiper mySwiper">
                <div class="swiper-wrapper">

                    @foreach (var g in (IEnumerable<EECBET.Models.GameListViewModel>)ViewBag.Exclusive)
                    {
                        <div class="swiper-slide" data-release-date="@g.ReleaseDate?.ToString("yyyy-MM-dd")">
                            <a href="#"><img src="@g.GameImageUrl" alt="@g.GameNameEN" /></a>
                        </div>
                    }

                </div>
                <!-- 左右箭頭 -->
                <div class="swiper-button-next"></div>
                <div class="swiper-button-prev"></div>
            </div>
        </section>

        <!-- 高報酬 -->
        <section>
            <div class="sectionTitle">
                <div>
                    <h3>高報酬</h3>
                </div>
                <div>
                    <a class="more" asp-controller="Games" asp-action="SlotGame_HighBonus">更多</a>
                </div>
            </div>


            <!-- Swiper -->
            <div class="swiper mySwiper">
                <div class="swiper-wrapper">
                    @if (ViewBag.HighBonusGames == null ||
                                        !((IEnumerable<EECBET.Models.GameListViewModel>)ViewBag.HighBonusGames).Any())
                    {
                        <p>⚠️ 尚無高報酬遊戲資料。</p>
                    }
                    else
                    {
                        foreach (var g in (IEnumerable<EECBET.Models.GameListViewModel>)ViewBag.HighBonusGames)
                        {
                            <div class="swiper-slide" data-release-date="@g.ReleaseDate?.ToString("yyyy-MM-dd")">
                                <a href="#">
                                    <img src="@g.GameImageUrl" alt="@g.GameNameEN" />
                                </a>
                            </div>
                        }
                    }
                </div>
                <!-- 左右箭頭 -->
                <div class="swiper-button-next"></div>
                <div class="swiper-button-prev"></div>
            </div>
        </section>

        <!-- 經典 -->
        <section>
            <div class="sectionTitle">
                <div>
                    <h3>經典</h3>
                </div>
                <div>
                    <a class="more" asp-controller="Games" asp-action="SlotGame_classic">更多</a>
                </div>

            </div>
            <!-- Swiper -->
            <div class="swiper mySwiper">
                <div class="swiper-wrapper">
                    @foreach (var g in (IEnumerable<EECBET.Models.GameListViewModel>)ViewBag.classic)
                    {
                        <div class="swiper-slide" data-release-date="@g.ReleaseDate?.ToString("yyyy-MM-dd")">
                            <a href="#"><img src="@g.GameImageUrl" alt="@g.GameNameEN" /></a>
                        </div>
                    }
                </div>
                <!-- 左右箭頭 -->
                <div class="swiper-button-next"></div>
                <div class="swiper-button-prev"></div>
            </div>
        </section>

        <!-- 頂級 -->
        <section>
            <div class="sectionTitle">
                <div>
                    <h3>頂級</h3>
                </div>
                <div>
                    <a class="more" asp-controller="Games" asp-action="SlotGame_Megaways">更多</a>
                </div>
            </div>
            <!-- Swiper -->
            <div class="swiper mySwiper">
                <div class="swiper-wrapper">
                    @foreach (var g in (IEnumerable<EECBET.Models.GameListViewModel>)ViewBag.Megaways)
                    {
                        <div class="swiper-slide" data-release-date="@g.ReleaseDate?.ToString("yyyy-MM-dd")">
                            <a href="#"><img src="@g.GameImageUrl" alt="@g.GameNameEN" /></a>
                        </div>
                    }
                </div>
                <!-- 左右箭頭 -->
                <div class="swiper-button-next"></div>
                <div class="swiper-button-prev"></div>
            </div>


        </section>
    </div>

    <!-- Swiper JS -->
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script src="~/js/slotgameHome.js" asp-append-version="true"></script>
</div>