@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>會員登入 - beteec</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+TC:wght@700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #0d0d0d, #1f1f1f);
            font-family: "Noto Serif TC", serif;
            color: #fff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .login-card {
            background: #222;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 255, 136, 0.5);
            padding: 40px;
            width: 400px;
        }

        .login-card h2 {
            text-align: center;
            margin-bottom: 30px;
            color: #1da867;
            font-size: 2rem;
        }

        .form-label {
            color: #ccc;
            margin-bottom: 8px;
        }

        .form-control {
            background: #333;
            border: 1px solid #555;
            color: #fff;
            padding: 12px;
            border-radius: 8px;
        }

        .form-control:focus {
            background: #444;
            border-color: #00ff88;
            box-shadow: 0 0 5px #00ff88;
            color: #fff;
        }

        .form-control::placeholder {
            color: #888;
        }

        .btn-login {
            background: linear-gradient(90deg, #006633, #1da867);
            border: none;
            color: #fff;
            font-weight: bold;
            padding: 12px;
            transition: 0.3s;
        }

        .btn-login:hover {
            background: linear-gradient(90deg, #004d26, #1da867);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(29, 168, 103, 0.3);
        }

        .links {
            text-align: center;
            margin-top: 20px;
        }

        .links a {
            color: #1da867;
            text-decoration: none;
            transition: color 0.3s;
        }

        .links a:hover {
            color: #00ff88;
            text-decoration: underline;
        }

        .captcha-box {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 10px;
        }

        .captcha-text {
            background: #333;
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: bold;
            letter-spacing: 3px;
            font-size: 20px;
            user-select: none;
            flex: 1;
            text-align: center;
            color: #1da867;
        }

        .btn-secondary {
            background: #555;
            border: none;
            color: #fff;
            padding: 8px 15px;
        }

        .btn-secondary:hover {
            background: #666;
        }

        .mb-3 {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="login-card">
        <h2>🎰 會員登入</h2>
        <form id="loginForm">
            <div class="mb-3">
                <label for="username" class="form-label">帳號</label>
                <input type="text" class="form-control" id="username" placeholder="輸入帳號" required>
            </div>
            
            <div class="mb-3">
                <label for="password" class="form-label">密碼</label>
                <input type="password" class="form-control" id="password" placeholder="輸入密碼" required>
            </div>
            
            <div class="mb-3">
                <label for="captchaInput" class="form-label">驗證碼</label>
                <div class="captcha-box">
                    <span id="captcha" class="captcha-text"></span>
                    <button type="button" class="btn btn-sm btn-secondary" onclick="generateCaptcha()">換一張</button>
                </div>
                <input type="text" class="form-control" id="captchaInput" placeholder="輸入上方驗證碼" required>
            </div>
            
            <button type="submit" class="btn btn-login w-100">登入</button>
        </form>

        <div class="links">
            <p>
                <a href="/Member/Register">註冊新帳號</a> | 
                <a href="#">忘記密碼?</a>
            </p>
            <p style="margin-top: 15px;">
                <a href="/">← 返回首頁</a>
            </p>
        </div>
    </div>

    <script>
        let currentCaptcha = "";

        function generateCaptcha() {
            const chars = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
            currentCaptcha = "";
            for (let i = 0; i < 4; i++) {
                currentCaptcha += chars.charAt(Math.floor(Math.random() * chars.length));
            }
            document.getElementById("captcha").textContent = currentCaptcha;
        }

        generateCaptcha();

        document.getElementById("loginForm").addEventListener("submit", function(e) {
            e.preventDefault();
            
            const username = document.getElementById("username").value.trim();
            const password = document.getElementById("password").value.trim();
            const userInput = document.getElementById("captchaInput").value.trim().toUpperCase();
            
            if (userInput !== currentCaptcha) {
                alert("驗證碼錯誤，請再試一次！");
                generateCaptcha();
                document.getElementById("captchaInput").value = "";
                return;
            }
            
            if (!username || !password) {
                alert("請填寫完整資料！");
                return;
            }
            
            alert("驗證成功，登入中...");
            // 這裡可以接後端 API 做登入
            // window.location.href = "/";
        });
    </script>
</body>
</html>